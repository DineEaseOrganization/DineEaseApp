# Required: react-native-phone-number-input@^2.1.0 declares loose peer deps
# (react: "*", react-native: "*") which npm 7+ rejects against React 19 / RN 0.81.
# Revisit once the package publishes updated peers or is replaced.
legacy-peer-deps=true
