/**
 * DineEase Design System — Single Export
 *
 * Usage:
 *   import { Colors, TextStyles, FontFamily, Spacing, Radius, Shadow } from '../../theme';
 */

export { Colors } from './colors';
export type { ColorKey } from './colors';

export {
  FontFamily,
  FontSize,
  LineHeight,
  LetterSpacing,
  TextStyles,
} from './typography';

export { Spacing, Radius, Shadow } from './spacing';
